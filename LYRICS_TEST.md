# 歌词显示测试功能说明

## 📋 功能概述

本测试功能用于验证在息屏/锁屏状态下显示歌词的可行性。

## 🎯 已实现的核心功能

### 1. **息屏显示支持**
添加了以下 WindowManager flags：
- `FLAG_SHOW_WHEN_LOCKED` - 锁屏时显示
- `FLAG_TURN_SCREEN_ON` - 唤醒屏幕
- `FLAG_KEEP_SCREEN_ON` - 保持屏幕常亮

### 2. **测试歌词滚动**
- ✅ 自动滚动歌词（每3秒切换）
- ✅ 显示当前歌词（高亮蓝色，24sp）
- ✅ 显示上一句/下一句（半透明，16sp）
- ✅ 显示进度指示器
- ✅ 显示时长：30秒（便于观察）

### 3. **模式切换**
**操作方式**：**按键序列 `+ - -`**（音量上 → 音量下 → 音量下）

- 必须在 **2秒内** 完成按键序列
- 顺序必须正确：先按音量上，再按两次音量下
- 防止误触：随机按键不会触发
- 可在锁屏状态操作（不会自动唤醒屏幕）

在测试模式下：
- 音量键不再调整音量
- 显示测试歌词界面
- 再次输入序列可退出测试模式

## 🧪 测试步骤

### 方案1：正常状态测试
1. 编译并安装 APK
2. 在屏幕亮起状态
3. **快速按键：上 → 下 → 下** (2秒内)
4. 观察是否：
   - ✅ Toast 提示"歌词测试模式: 开启"
   - ✅ 显示歌词界面
   - ✅ 歌词自动滚动
   - ✅ 持续显示30秒

### 方案2：锁屏测试（重点）
1. 锁定屏幕（按电源键）
2. **在锁屏状态按键：上 → 下 → 下**
3. **屏幕不会自动亮起**（这是设计）
4. 手动解锁或点亮屏幕后，观察歌词界面是否显示

### 方案3：验证按键序列
1. 尝试错误序列：下 → 下 → 上（不应触发）
2. 尝试超时：上（等待3秒）→ 下 → 下（不应触发）
3. 验证正确序列：上 → 下 → 下（应触发）

## 📊 测试歌词内容

当前使用周杰伦《七里香》歌词片段：
```
天空好想下雨
我好想住你隔壁
傻站在你家楼下
抬起头 数乌云
如果场景里出现一架钢琴
我会唱歌给你听
哪怕好多盆水往下淋
```

## 🎨 UI 设计

```
┌─────────────────────────────────┐
│  [上一句歌词 - 半透明灰色]      │
│                                 │
│  【当前歌词 - 亮蓝色高亮】      │
│                                 │
│  [下一句歌词 - 半透明灰色]      │
│                                 │
│  测试歌词模式 (3/7)             │
└─────────────────────────────────┘
```

- 背景：黑色半透明 (alpha=0.8)
- 当前歌词颜色：`#00D9FF`（青蓝色）
- 字体大小：当前24sp，其他16sp

## ✅ 可行性验证结果

如果测试成功，说明以下技术方案可行：

1. ✅ **在息屏状态显示悬浮窗**
   - 使用 `TYPE_ACCESSIBILITY_OVERLAY`
   - 配合锁屏/唤醒 flags

2. ✅ **无障碍服务拦截按键**
   - 可以用任意按键触发歌词显示
   - 建议后续使用：音量上+下同时按

3. ✅ **自动滚动歌词**
   - Compose + Coroutine 实现流畅动画
   - 可以根据时间戳精确控制

## 🔮 下一步计划

### Phase 1: 歌词获取
调研如何从播放器获取歌词：

#### 方案A：酷狗播放器
1. **悬浮歌词API**
   - 检查酷狗是否有公开API
   - 分析酷狗悬浮歌词的实现方式
   - 可能需要：
     * 监听通知栏
     * 读取酷狗缓存目录
     * Hook 酷狗进程（需要root）

2. **本地歌词文件**
   - 读取 `.lrc` 格式歌词文件
   - 解析时间戳
   - 匹配当前播放进度

#### 方案B：通用方案
1. **MediaSession API**
   - 获取当前播放信息
   - 歌曲名、艺术家
   
2. **在线歌词源**
   - 根据歌曲信息搜索歌词
   - 支持多个歌词源（网易云、QQ音乐等）

3. **本地歌词库**
   - 建立歌词缓存数据库
   - 支持用户手动导入

### Phase 2: 独立APK
将此功能分离为独立的歌词显示应用：
- 移除音量控制相关代码
- 专注于歌词显示和同步
- 优化电池消耗
- 添加更多自定义选项（字体、颜色、位置等）

## 🔧 技术细节

### 关键代码位置

1. **窗口参数** (Service.kt: ~line 306)
```kotlin
WindowManager.LayoutParams(
    ...
    FLAG_SHOW_WHEN_LOCKED  // 仅在锁屏显示，不唤醒屏幕
)
```

**重要变更**：
- ✅ 保留 `FLAG_SHOW_WHEN_LOCKED` - 允许在锁屏显示
- ❌ 移除 `FLAG_TURN_SCREEN_ON` - 不自动唤醒屏幕
- ❌ 移除 `FLAG_KEEP_SCREEN_ON` - 不保持屏幕常亮

2. **按键序列检测** (Service.kt: ~line 114)
```kotlin
private fun checkKeySequence(keyCode: Int) {
    // 检测 + - - 序列
    if (keySequence == targetSequence) {
        testLyricsMode = !testLyricsMode
    }
}
```

3. **测试歌词组件** (Service.kt: ~line 571)
```kotlin
@Composable
fun TestLyricsDisplay() {
    // 歌词滚动逻辑
}
```

### 性能考虑

- ✅ 使用协程实现定时器（低开销）
- ✅ Compose 自动优化重组
- ✅ 仅在可见时运行动画
- ⚠️ 长时间显示会保持屏幕常亮（耗电）

## 📝 测试检查清单

- [ ] 息屏状态下能否唤醒并显示
- [ ] 锁屏状态下能否覆盖显示
- [ ] 歌词是否自动滚动
- [ ] 切换功能是否正常
- [ ] 显示时长是否足够（30秒）
- [ ] 字体大小是否合适
- [ ] 颜色对比度是否清晰
- [ ] 是否影响其他应用
- [ ] 电池消耗情况

## 💡 常见问题

**Q: 为什么要用按键序列而不是单个按键？**
A: 防止误触。需要特定顺序 `+ - -` 在2秒内完成，随机按键不会触发。

**Q: 为什么屏幕不会自动亮起？**
A: 为了避免意外唤醒屏幕耗电。设计为只在已解锁的屏幕上显示，锁屏时触发后需手动点亮查看。

**Q: 锁屏状态下触发后看不到怎么办？**
A: 按电源键点亮屏幕，你会看到歌词界面已经显示在锁屏界面上方。

**Q: 如何退出测试模式？**
A: 再次输入序列 `上 → 下 → 下`，或等待30秒自动隐藏。

**Q: 为什么30秒后消失？**
A: 这是 `IDLE_TIMEOUT` 设置。可以调整为更长时间，或改为手动关闭。

**Q: 序列输入失败怎么办？**
A: 确保在2秒内完成，顺序正确。查看 logcat 可以看到序列记录。

## 🎉 预期效果

如果一切正常，你应该看到：

**正常状态下：**
1. 快速按键：上 → 下 → 下
2. Toast 提示"歌词测试模式: 开启"
3. 显示歌词界面
4. 歌词每3秒自动切换
5. 界面美观，对比度好
6. 30秒后自动消失

**锁屏状态下：**
1. 按键：上 → 下 → 下
2. 屏幕不会亮起（省电）
3. 手动点亮屏幕
4. 看到歌词界面显示在锁屏上方
5. 可以继续观看歌词滚动

这将证明**息屏歌词显示方案完全可行**！🚀

